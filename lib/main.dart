import 'package:flutter/material.dart';
import 'package:flutter_on_december/1202/beverage_app.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: BeverageApp(),
    );
  }
}